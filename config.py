#config.py

# Path to the directory containing the training and test data
data_directory = './training_test_data'

# Path to the base directory of your code
code_directory = './'

# Path to the directory where you want to save the results of training
training_results_directory = './saved_models'
training_results_filename = './saved_models/training_results.joblib'